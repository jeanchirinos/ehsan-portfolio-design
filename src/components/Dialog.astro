<dialog class='with-padding'>
  <form>
    <h3>Contact me 👋</h3>
    <div>
      <input type='email' name='email' placeholder=' ' required id='contact-email' />
      <label for='contact-email'>Email</label>
    </div>
    <div>
      <textarea cols='30' rows='4' placeholder=' ' required id='contact-message'></textarea>
      <label for='contact-message'>Message</label>
    </div>

    <button class='btn'>Send</button>
  </form>
</dialog>

<script>
  import './Dialog'
</script>

<style lang='scss'>
  @use '../scss/mixins' as *;

  dialog {
    border: none;
    padding: 0;
    background-color: rgba(0, 0, 0, 0.3);
    position: fixed;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100vh;
    top: 0;
    left: 0;
    @include visibility(hidden);
    transition: background-color 0.2s ease-in-out, opacity 0.2s ease-in-out,
      visibility 0.2s ease-in-out;

    &.open {
      @include visibility(visible);

      form {
        transform: scale(1);
      }
    }
  }

  form {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    row-gap: 2rem;
    background-color: white;
    border-radius: 0.5rem;
    transform: scale(0.5);
    transition: transform 0.2s ease-in-out, opacity 0.2s ease-in-out;
  }

  h3 {
    text-align: center;
    margin-bottom: 1rem;
  }

  div {
    position: relative;
  }

  label {
    background-color: white;
    color: gray;
    padding-inline: 0.3rem;
    position: absolute;
    top: 0.5rem;
    left: 0.5rem;
    transition: transform 0.2s ease-in-out;
    pointer-events: none;
    transform: translateY(-20px);
  }

  :placeholder-shown:not(:focus) + label {
    transform: translateY(0px);
  }

  input,
  textarea {
    width: 100%;
    font: inherit;
    font-size: 0.9rem;
    border-radius: 0.2rem;
    border: 1px solid gray;
    padding-block: 1rem 0.5rem;
    padding-inline: 0.5rem;
  }

  input:-webkit-autofill + label {
    background: linear-gradient(to top, #e8f0fe 50%, white 50%);
  }

  textarea {
    resize: none;
  }
</style>
